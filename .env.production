# ── Dialectix Frontend — PRODUCTION environment ───────────────────────────────
# Loaded automatically by Vite when running `vite build`.
# Replace the URL below with your actual deployed backend domain.
#
# ⚠️  NO API KEYS HERE — the Anthropic key lives only in the backend .env files.

# URL of the NestJS AI backend gateway (production deployment)
VITE_BACKEND_URL=https://your-backend-domain.com
